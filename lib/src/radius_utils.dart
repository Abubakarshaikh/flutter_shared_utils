import 'package:flutter/material.dart';

abstract class RadiusUtils {
  static const Radius k2Radius = Radius.circular(2);
  static const Radius k4Radius = Radius.circular(4);
  static const Radius k6Radius = Radius.circular(4);
  static const Radius k12Radius = Radius.circular(12);
  static const Radius k10Radius = Radius.circular(12);
}
